@* Views/Custom/AttributeRoutingPage.cshtml *@
@{
    ViewData["Title"] = ViewBag.Title;
}

<div class="card bg-white rounded-xl shadow-lg p-6 mb-6">
    <h1 class="text-4xl font-bold text-gray-800 mb-4">@ViewData["Title"]</h1>
    <p class="text-lg text-gray-700">This page is accessed using **attribute routing**, where the route is defined directly on the action method (e.g., `/dummy-pages/custom-attribute-page`).</p>
</div>
