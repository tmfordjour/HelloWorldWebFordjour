@* Views/Admin/Index.cshtml *@
@{
    ViewData["Title"] = ViewBag.Title;
}

<div class="card bg-blue-50 border border-blue-200 rounded-xl shadow-lg p-6 mb-6">
    <h1 class="text-4xl font-bold text-blue-800 mb-4">@ViewData["Title"]</h1>
    <p class="text-lg text-blue-700">This is the dedicated Admin Area.</p>
</div>
